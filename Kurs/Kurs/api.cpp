#include "api.h"

API::API(QObject* parent)
    : QObject(parent)
{

}

void API::setInputData(int i, QString vec){
    //block_list.value(i)->setInputData(vec);
}

void API::setOutputData(int i, QString vec){
    //block_list.value(i)->setOutputData(vec);
}
